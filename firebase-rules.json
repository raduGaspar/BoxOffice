{
  "rules": {
    ".read": true,
    "statuses": {
      ".write": false
    },
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        "shows": {
          ".indexOn": ["name", "tags"]
        },
        "settings": {}
      }
    }
  }
}